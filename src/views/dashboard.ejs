<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | TechAura Shipments</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<%- include('partials/header') %>

<div class="app-container">
    <%- include('partials/sidebar', { currentPage: 'dashboard' }) %>
    
    <main class="main-content">
        <div class="dashboard">
            <h1>📊 Dashboard</h1>
            
            <div class="stats-row">
                <div class="stat-card">
                    <h3>📦 Pedidos pendientes</h3>
                    <span class="number"><%= stats.pending %></span>
                </div>
                <div class="stat-card">
                    <h3>📬 Guías enviadas hoy</h3>
                    <span class="number"><%= stats.sentToday %></span>
                </div>
                <div class="stat-card">
                    <h3>❌ Errores de envío</h3>
                    <span class="number error"><%= stats.errors %></span>
                </div>
            </div>

            <div class="whatsapp-section" style="margin: 1.5rem 0; padding: 1.2rem; background: #f0fff4; border: 1px solid #25D366; border-radius: 8px;">
                <h2 style="margin-top:0;">📱 Crear Guías desde WhatsApp</h2>
                <p>Sube capturas de pantalla de conversaciones de WhatsApp para extraer automáticamente los datos de los clientes y generar órdenes masivas.</p>
                <a href="/orders/confirm" class="btn btn-primary" style="display:inline-block; margin-top:0.5rem;">
                    📷 Subir capturas y extraer pedidos
                </a>
            </div>
            
            <div class="recent-section">
                <h2>Últimos pedidos</h2>
                <% if (orders && orders.length > 0) { %>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Pedido</th>
                            <th>Cliente</th>
                            <th>Ciudad</th>
                            <th>Estado</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% orders.forEach(function(order) { %>
                        <tr>
                            <td><%= order.orderNumber %></td>
                            <td><%= order.customerName %></td>
                            <td><%= order.city || 'N/A' %></td>
                            <td><span class="badge <%= order.status %>"><%= order.status %></span></td>
                            <td class="actions-cell">
                                <a href="/orders/<%= order.orderNumber %>" class="btn btn-sm">Ver</a>
                                <button onclick="uploadGuide('<%= order.orderNumber %>')" class="btn btn-primary btn-sm">
                                    Subir guía
                                </button>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
                <% } else { %>
                <div class="empty-state-container">
                    <p class="empty-state">⚠️ Sin conexión a base de datos o al bot de TechAura.</p>
                    <p>El sistema funciona en modo standalone para procesar guías de WhatsApp. Conecta la base de datos TechAura para ver pedidos automáticos.</p>
                </div>
                <% } %>
            </div>
        </div>
    </main>
</div>

<script src="/js/dashboard.js"></script>
</body>
</html>
